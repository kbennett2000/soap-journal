"""Top-level operator CLI for soap-journal.

Subcommands:

- `load-translation <canonical.json>` — ingest canonical Bible JSON into the
  configured database.

Invoked via `python -m soap_journal.cli <subcommand> ...`.
"""
