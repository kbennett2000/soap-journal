# TODO: Define the canonical Bible JSON schema here.
#
# This module will be the single source of truth for the normalized Bible
# format that all parsers emit and the loader consumes. Per SPEC.md §4.2 it
# must accommodate at minimum: section headings, footnotes, and a red-letter
# flag per verse. The schema is deferred to the Bible-parser task — do not
# design it ahead of that conversation.
